# Install Target

This directory is reserved for a Claude Code copy of the canonical skill.

Canonical source:

```text
skills/wordpress-plugin-dev/
```

Run `npm run sync:targets` in an unrestricted local environment to copy the full skill here.
